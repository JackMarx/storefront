<h1>Add Image to <%= @product.name %></h1>

<%= form_tag "/products/#{@product.id}/images" do %>
  <div class="form-group">
    <%= label_tag :url %>
    <%= text_field_tag :url %>
  </div>
  <%= submit_tag "new", class: "btn btn-default" %>
<% end %>